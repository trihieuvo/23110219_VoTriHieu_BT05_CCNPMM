info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/v1/api/login
  auth: none
  body: json

json:
  email: "test@example.com"
  password: "password123"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
