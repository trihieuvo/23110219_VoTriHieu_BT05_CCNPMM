info:
  name: GetAccount
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/v1/api/account
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
