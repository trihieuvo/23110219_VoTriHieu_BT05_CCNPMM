info:
  name: GetMostViewed
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8080/v1/api/products/most-viewed
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
