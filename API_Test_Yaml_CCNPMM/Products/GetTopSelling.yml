info:
  name: GetTopSelling
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/v1/api/products/top-selling
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
